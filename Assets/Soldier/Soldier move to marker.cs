using UnityEngine;

public class SoldierMoveTomarker : MonoBehaviour
{
    public SoldierMarker sm;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
